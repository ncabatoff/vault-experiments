{
  "service": {
    "name": "vault",
    "port": 8200,
    "checks": [
      {
        "id": "vault",
        "service_id": "vault",
        "name": "Vault HTTP API",
        "http": "http://localhost:8200/v1/sys/health",
        "interval": "5s",
        "timeout": "1s"
      }
    ]
  }
}
